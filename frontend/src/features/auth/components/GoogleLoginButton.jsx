// Google OAuth login has been removed in favour of real Keycloak username/password auth.
// This stub remains only so any lingering imports keep resolving.
const GoogleLoginButton = () => null;

export default GoogleLoginButton;
